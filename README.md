# Encryptor Voice — Telegram-бот для расшифровки голосовых сообщений

Encryptor Voice — простой Telegram-бот, который принимает голосовые сообщения и преобразует их в текст. Бот использует pydub для конвертации OGG→WAV и библиотеку SpeechRecognition (Google Web Speech API) для распознавания речи. Проект распространяется под лицензией Apache 2.0 (см. файл LICENSE).

## Возможности
- Принимает голосовые сообщения из чатов Telegram
- Конвертирует OGG (Telegram) в WAV с помощью FFmpeg
- Отправляет пользователю расшифрованный текст
- Удаляет временные аудиофайлы после обработки

## Требования
- Python 3.8+
- FFmpeg (должен быть установлен в системе и доступен в PATH)
- Интернет (для доступа к Google Web Speech API)
- Зависимости из `requirements.txt`

Основные Python-библиотеки:
- aiogram — Telegram bot framework
- SpeechRecognition — распознавание речи
- pydub — конвертация аудио
- python-dotenv — загрузка переменных окружения

## Установка

1. Клонируйте репозиторий:
   git clone https://github.com/Kan1es/encryptor_voice.git
   cd encryptor_voice

2. Установите системную зависимость: FFmpeg.
   - На Ubuntu/Debian:
     sudo apt update && sudo apt install ffmpeg
   - На macOS (Homebrew):
     brew install ffmpeg
   - На Windows — скачайте сборку FFmpeg и добавьте в PATH.

3. Установите Python-зависимости:
   python -m pip install -r requirements.txt

4. Создайте файл `.env` в корне проекта и добавьте токен бота:
   BOT_TOKEN=123456:ABC-DEF...

## Быстрый запуск
Запустите бота локально:
python main.py

Бот начнёт polling и будет обрабатывать входящие голосовые сообщения.

## Примеры использования
1. Откройте диалог с ботом в Telegram.
2. Отправьте голосовое сообщение (стандартный формат Telegram — OGG/OPUS).
3. Дождитесь ответа бота с расшифровкой текста. В процессе будет показано сообщение о конвертации и распознавании.

## Как это работает (коротко)
- Бот скачивает голосовое сообщение из Telegram (OGG).
- pydub конвертирует OGG в WAV с использованием FFmpeg.
- SpeechRecognition загружает WAV и отправляет аудиоданные в Google Web Speech API для распознавания речи (language="ru-RU" по умолчанию).
- Результат отправляется обратно пользователю. Временные файлы удаляются.

## Отладка и распространённые ошибки
- "Не найдена программа FFmpeg" — убедитесь, что FFmpeg установлен и доступен в PATH. На Windows добавьте путь к ffmpeg.exe в переменные окружения.
- BOT_TOKEN не задан — проверьте, что `.env` содержит корректный BOT_TOKEN.
- "Не удалось распознать речь" — качество аудио или фоновые шумы могут мешать распознаванию.
- Ограничения Google Web Speech API: бесплатный метод может иметь ограничения по длительности/частоте запросов и точности.

## Конфиденциальность
Аудиофайлы временно сохраняются на сервере для конвертации и распознавания, затем удаляются (см. main.py). Распознавание выполняется с использованием Google Web Speech API — аудиоданные отправляются в сторонний сервис для обработки. Убедитесь, что вы уведомили пользователей об этом, если обрабатываете конфиденциальные данные.

## Разработка и вклад
Если хотите улучшить проект:
- Добавьте поддержку других движков распознавания (например, локальные модели или платные API с ключами).
- Обработку длинных аудиосообщений (разбиение на куски).
- Настройки языка через команду бота.

Пулл-реквесты и issue приветствуются.

## Лицензия
Проект лицензирован под Apache License 2.0 — см. файл LICENSE для полного текста.
